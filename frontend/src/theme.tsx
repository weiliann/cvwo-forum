import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4267b2",
    },
    secondary: {
      main: "#EDF0F6",
    }
  },
})

export default theme;